namespace AQ.Utilities.Sort;

/// <summary>
/// Supported sort directions for dynamic sorting
/// </summary>
public enum SortDirection
{
    /// <summary>
    /// Ascending order (A-Z, 0-9, oldest to newest)
    /// </summary>
    Ascending,

    /// <summary>
    /// Descending order (Z-A, 9-0, newest to oldest)
    /// </summary>
    Descending
}
